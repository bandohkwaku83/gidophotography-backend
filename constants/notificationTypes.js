/** In-app admin notification kinds (persisted on Notification). */
export const NOTIFICATION_TYPES = [
    "selection_add",
    "selection_remove",
    "selection_sync",
    "selection_submit",
    "final_download",
    "booking_reminder_admin",
]
